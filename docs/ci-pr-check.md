PR check: SonarCloud

Este archivo existe solo para disparar el pipeline en un Pull Request y verificar:
- Decoración del PR en SonarCloud.
- Publicación de cobertura combinada (JaCoCo + LCOV).
- Quality Gate en el job Analyze.

Se puede eliminar tras validar la configuración.