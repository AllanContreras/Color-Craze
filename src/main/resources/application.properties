spring.application.name=Color-craze

spring.data.mongodb.uri=mongodb+srv://allancontreras009_db_user:lC01qlaIsJnI4CfX@cluster0.woazokj.mongodb.net/Color-Craze?retryWrites=true&w=majority&serverSelectionTimeoutMS=5000&connectTimeoutMS=5000&socketTimeoutMS=5000

# Puerto del servidor
server.port=8080


#logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
#por alguna razon me dejo todo menos la contraseña como valores validos

# Config CORS
app.cors.allowed-origins=http://localhost:3000,https://eci-bienestar.edu.co
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=Authorization,Content-Type,X-Requested-With
logging.level.org.mongodb.driver.cluster=WARN
logging.level.org.mongodb.driver.connection=WARN
logging.level.org.mongodb.driver.client=WARN

# Feature flags
app.guest.persist=false
app.auth.persist=false

# Actuator & Prometheus metrics exposure
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.prometheus.enabled=true
management.metrics.tags.application=ColorCraze
# Latency timer histogram + percentiles for game moves
management.metrics.distribution.percentiles-histogram.game.move.latency=true
management.metrics.distribution.percentiles.game.move.latency=0.5,0.95,0.99

# Redis (optional)
# redis.host=localhost
# redis.port=6379
# TTL de snapshots (segundos)
redis.ttl.seconds=86400

# STOMP broker relay (set enabled=true when external broker like RabbitMQ is available)
colorcraze.stomp.relay.enabled=false
colorcraze.stomp.relay.host=localhost
colorcraze.stomp.relay.port=61613
colorcraze.stomp.relay.clientLogin=guest
colorcraze.stomp.relay.clientPasscode=guest
colorcraze.stomp.relay.systemLogin=guest
colorcraze.stomp.relay.systemPasscode=guest
colorcraze.stomp.relay.virtualHost=/

# Logging
logging.level.com.Color_craze=INFO

# Admin (opcional, solo local)
# admin.user=admin
# admin.pass=changeme
